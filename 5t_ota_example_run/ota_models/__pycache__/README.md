# Python Bytecode Cache

This directory was created automatically when the generated OTA model modules were imported. The `.pyc` files cache Python bytecode and do not contain new equations, sizing decisions, or verification results.

The cache is disposable and can be omitted from a published example. Python will recreate it as needed from the parent directory's `.py` model files.
